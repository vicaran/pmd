<?xml version="1.0"?>

<ruleset name="Empty Code"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_2_0_0.xsd">

    <description>
        Rules to detect empty blocks of code
    </description>

    <rule name="EmptyIfStmt"
          language="ecmascript"
          since="0.1"
          message="Avoid empty 'if' statements"
          class="net.sourceforge.pmd.lang.ecmascript.rule.emptycode.EmptyIfStmt"
          externalInfoUrl="${pmd.website.baseurl}/pmd_rules_ecmascript_emptycode.html#emptyifstmt">
        <description>
            Empty If Statement finds instances where a condition is checked but nothing is done about it.
        </description>
        <priority>1</priority>
        <example>
            <![CDATA[
function Foo(x){

  if (x == 0) {
   // empty!
  }
}
]]>
        </example>
    </rule>

    <rule name="EmptyCatchBlock"
          language="ecmascript"
          since="0.1"
          message="Avoid empty 'catch' statements"
          class="net.sourceforge.pmd.lang.ecmascript.rule.emptycode.EmptyCatchBlock"
          externalInfoUrl="${pmd.website.baseurl}/pmd_rules_ecmascript_emptycode.html#emptycatchblock">
        <description>
            Empty Catch Block finds instances where an exception is caught, but nothing is done.
            In most circumstances, this swallows an exception which should either be acted on or reported.
        </description>
        <priority>1</priority>
        <example>
            <![CDATA[
function Foo(x){

  if (x == 0) {
   // empty!
  }
}

function Foo(x){
    try {
        adddlert("Hello, World!");
    }
    catch(err) {
        // empty!
    }
}
]]>
        </example>
    </rule>
</ruleset>